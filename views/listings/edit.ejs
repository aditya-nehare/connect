<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Listing</title>
</head>
<body>
  <h2>Edit Listing</h2>

  <!-- Form for editing a listing -->
  <form method="POST" action="/listing/<%= listing._id %>?_method=PUT">
    <label>
      Title:
      <input
        name="listing[title]"
        type="text"
        value="<%= listing.title %>"
        required
      />
    </label>
    <br />

    <label>
      Description:
      <textarea name="listing[description]" required><%= listing.description %></textarea>
    </label>
    <br />

    <label>
      Image URL:
      <input
        name="listing[image]"
        type="text"
        value="<%= listing.image %>"
        placeholder="Enter image URL"
      />
    </label>
    <br />

    <label>
      Price:
      <input
        name="listing[price]"
        type="number"
        value="<%= listing.price %>"
        min="0"
        step="0.01"
        required
      />
    </label>
    <br />

    <label>
      Location:
      <textarea name="listing[location]" required><%= listing.location %></textarea>
    </label>
    <br />

    <button type="submit">Update Listing</button>
  </form>
</body>
</html>
